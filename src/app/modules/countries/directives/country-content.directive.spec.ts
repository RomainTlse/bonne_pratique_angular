import { CountryContentDirective } from './country-content.directive';

describe('CountryContentDirective', () => {
  it('should create an instance', () => {
    const directive = new CountryContentDirective();
    expect(directive).toBeTruthy();
  });
});

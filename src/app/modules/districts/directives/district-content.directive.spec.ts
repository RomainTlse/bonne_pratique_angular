import { DistrictContentDirective } from './district-content.directive';

describe('DistrictContentDirective', () => {
  it('should create an instance', () => {
    const directive = new DistrictContentDirective();
    expect(directive).toBeTruthy();
  });
});
